import React from 'react';
import { shallow } from 'enzyme';
import expect from 'expect';
import toJson from 'enzyme-to-json';

import { Map } from 'immutable';

// import { CaloriesList } from './List';

// function setup() {
//   const props = {
//     calories: Map(),

//     CalorieActions: {},
//   };

//   return shallow(<CaloriesList {...props} />);
// }

describe('CaloriesList  Unit Tests', () => {
  describe('', () => {
    // const wrapper = setup();
    it('renders correctly', () => {
      // const jsonOutput = toJson(wrapper);
      // // expect(jsonOutput).toMatchSnapshot();
      // expect(wrapper.find("div[data-role='calories-list']")).toHaveLength(1);
    });
  });
});
